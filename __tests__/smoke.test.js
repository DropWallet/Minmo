/**
 * Smoke tests for MinMo
 * These are basic sanity checks to ensure the test setup works
 */

describe('MinMo Smoke Tests', () => {
  it('test runner works', () => {
    expect(true).toBe(true);
  });

  it('basic math works', () => {
    expect(1 + 1).toBe(2);
  });
});

